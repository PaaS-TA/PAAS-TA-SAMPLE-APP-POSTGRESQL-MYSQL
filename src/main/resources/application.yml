spring:
  jpa:
    generate-ddl: true
    show-sql: true
    properties:
      hibernate.temp.use_jdbc_metadata_defaults: false

server:
  port: 8888

postgresql:
  datasource:
    driver-class-name: org.postgresql.Driver
    jdbc-url: ${POSTGRES_URL}
#    username: ${POSTGRES_USERNAME}
#    password: ${POSTGRES_PW}
#    jdbc-url: jdbc:postgresql://115.68.47.174:30465/postgres
#    jdbc-url: jdbc:postgresql://45.77.19.223:5524/postgres
    username: postgres
    password: q

mysql: 
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
#    jdbc-url: ${MYSQL_URL}
#    username: ${MYSQL_USERNAME}
#    password: ${MYSQL_PS}
    jdbc-url: jdbc:mysql://45.77.19.223:3306/test
    username: root
    password: q
